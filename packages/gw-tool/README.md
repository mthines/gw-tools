# gw-tool

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build gw-tool` to build the library.

## Running unit tests

Run `nx test gw-tool` to execute the unit tests via [Jest](https://jestjs.io).
