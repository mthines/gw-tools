---
paths: src/**/*.ts
---

# TypeScript Guidelines

- Use `.js` extensions in imports (ES module resolution)
- Export types with `export type` for type-only exports
- Keep `AgentDefinition` and `ToolName` types in sync with Claude Agent SDK
