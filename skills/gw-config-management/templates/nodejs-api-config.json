{
  // ============================================================================
  // gw Configuration Template: Node.js API/Backend
  // ============================================================================
  // Documentation: https://github.com/mthines/gw-tools
  // ============================================================================

  // Absolute path to repository root (parent directory containing all worktrees)
  // UPDATE THIS to match your project path
  "root": "/path/to/your/api.git",

  // Source worktree to copy files from (usually 'main' or 'develop')
  "defaultBranch": "main",

  // Files/directories to automatically copy when creating new worktrees
  "autoCopyFiles": [
    ".env",             // Environment variables (DATABASE_URL, JWT_SECRET, API_KEYS)
    "ssl/",             // SSL certificates and private keys for HTTPS
    "keys/",            // JWT signing keys, encryption keys, service account keys
    "secrets/",         // API secrets, service credentials, third-party tokens
    "config/local.json", // Local configuration overrides (ports, debug settings)
  ],

  // ============================================================================
  // SETUP INSTRUCTIONS
  // ============================================================================
  // 1. Update 'root' to your actual path
  // 2. Never auto-copy: node_modules/, dist/, build/, coverage/
  // 3. Consider adding: prisma/.env, .aws/, .gcp/, docker/.env
  // ============================================================================
}
